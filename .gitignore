# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
*.egg-info/

# Database
*.db
*.db-wal
*.db-shm

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Test / coverage
.coverage
.pytest_cache/
htmlcov/
tests/ui/screenshots/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets / local config
.env
*.env.local

# Frontend (Vite + npm) — dist/ is rsync'd to the Pi but never committed
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/.eslintcache
# TypeScript incremental-build caches; one per tsconfig.*.json
*.tsbuildinfo

# Playwright (HTML report + intermediate per-run artefacts)
playwright-report/
test-results/

# Internal / private project material (not for public repo)
internal_docs/

# Session continuity (internal, not for public repo)
NOTES.md
scratchpad.md

# Local dev overrides
CLAUDE.local.md

# Claude.md backup artifacts
CLAUDE.old.md

# Claude Code (AI assistant local context — not for public repo)
.claude/
CLAUDE.md
